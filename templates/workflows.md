# 业务流程

## 旅程
每条旅程写出自然语言触发、页面入口、明确用户意图、前置条件、读取依据、提案、批准、提交、可观察终态。

## 状态与转换
写入流程至少区分 proposed、approved、executing、committed、failed、unknown。每条边指定服务端谓词、持久记录、超时处理和恢复责任人。

## 业务不变量
列出必须恒真的规则，以及强制规则的数据库约束/服务端代码和测试 case；prompt 不作为执行控制。

## 竞争与恢复
同一实体两提案、重复点击、审批后数据变化、提交丢回包、取消/刷新、多 worker、外部回调、批次部分完成分别如何处理？
