# 更新记录

## 0.2.0 · 2026-09-20

- 增加可选 `agui_runtime`，复用单个 SQLite 数据库内的提案、批准、期限/撤销、原子业务写与回执；用库存和预约两个领域验证。
- 增加自动 Playwright runner、实际页面 suite、截图/trace 与错误 UI 负控；增加结构化精确匹配评测器及独立人员配对基线接口。离线模型示例不产生真实质量 PASS。
- Harness 对 ui/model_eval 强制要求原始 artifacts，并在运行后及每次 gate 核对文件摘要。这是适配器迁移要求；旧证据必须重新生成。
- 增加仓库实际 GitHub Actions、失败聚合门禁、正负控和工件归档，以及只读分支保护核对脚本。CI 通过不等于仓库规则已设置，也不等于应用发布通过。

## 0.1.1 · 2026-09-20

会议室应用实际使用中发现，从目标工程目录通过包含`..`的路径运行harness，engine_hash没有规范化`__file__`，导致relative_to异常，无法执行检查。现在先resolve入口路径，并新增从目标工程启动真实CLI/adapter的回归。40个自动测试通过。

不改变业务控制/证据格式，也不声称旧证据仍有效；引擎哈希改变后目标工程应重新生成运行证据。本版本未全局安装或发布。
